{
  "name": "webrtc-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "start": "vite preview --port $PORT --https"
  },
  "devDependencies": {
    "vite": "^2.6.2"
  },
  "dependencies": {
    "esbuild": "^0.13.3",
    "firebase": "^8.10.0"
  }
}
